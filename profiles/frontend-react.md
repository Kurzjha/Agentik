# React Frontend Rules

- Build UI from small, composable components.
- Keep state local when possible and lift it only when needed.
- Use predictable data flow and explicit props.
- Respect responsive layout and accessibility from the start.
- Validate visible rendering changes after implementation.
